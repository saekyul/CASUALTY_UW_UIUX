version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: casualty_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-casualty_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-casualty_password}
      POSTGRES_DB: ${POSTGRES_DB:-casualty_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-casualty_user}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - casualty_network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: casualty_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@example.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - casualty_network

volumes:
  postgres_data:
    driver: local

networks:
  casualty_network:
    driver: bridge
